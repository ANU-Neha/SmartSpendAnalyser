/** @type {import('next').NextConfig} */ // This is an optional JSDoc for type checking
const nextConfig = {
  output: 'export'
  /* You can add other config options here if needed later */
};

// This is the standard CommonJS export that Next.js expects for next.config.js
module.exports = nextConfig;